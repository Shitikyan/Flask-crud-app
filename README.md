# Python web back-end CRUD application

Simple web back-end CRUD application written in Flask using SQLAlchemy dockerized database.

## Setup

To set up the project run the command bellow:
```zsh
docker-compose up --build
```

## Run
```zsh
  cd app && flask run
```